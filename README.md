# Redmine issues to MQTT

Very basic plugin to send issue metadata over MQTT.

## Configuration

Make sure you set host and topic in the plugin settings.

## NOTE

This only support basic MQTT without SSL support (for now).
